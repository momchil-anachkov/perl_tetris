use strict;
use warnings;

package Colors;

use constant background => (255,255,255);
use constant block_default => (30,30,30);
use constant pen => (60,60,60);

=comment
Default mino colors.
=cut
#use constant j_mino => (0,0,255);
#use constant l_mino => (255,165,0);
#use constant s_mino => (0,255,0);
#use constant z_mino => (255,0,0);
#use constant t_mino => (160,32,240);
#use constant i_mino => (0,255,255);
#use constant o_mino => (255,255,0);

=comment
Funky mino colors.
=cut
use constant j_mino => (250,5,5);
use constant l_mino => (227,23,213);
use constant s_mino => (250,250,65);
use constant z_mino => (255,167,3);
use constant t_mino => (2,245,6);
use constant i_mino => (2,245,241);
use constant o_mino => (26,217,175);

1;